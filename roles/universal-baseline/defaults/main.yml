---
admin_group_map:
  RedHat: wheel
  Debian: sudo
  Suse: root

baseline_packages:
   - vim
   - git
   - wget

baseline_packages_redhat:
  - vim-enhanced

baseline_packages_al2023:
  - curl-minimal

timezone: "America/New_York"

baseline_users:
  - name: harrycallahan
    uid: 1051
    groups: wheel
    shell: /bin/bash
    state: present
